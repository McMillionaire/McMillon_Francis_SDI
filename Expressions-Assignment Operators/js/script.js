/**
 * Created by McMillionaire on 1/18/15.
 */
//An Expression with Assignment

var a = 3;
a = a + 4;
console.log(a)

//assignment operator += example a += 4;, which is the same as a = a + 4
// a *= multiplication, a -=, a /=

//increment and decrement operators  (only works with the number 1) ++ Plus, -- minus
